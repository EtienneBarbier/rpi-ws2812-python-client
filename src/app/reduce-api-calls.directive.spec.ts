import { ReduceApiCallsDirective } from './reduce-api-calls.directive';

describe('ReduceApiCallsDirective', () => {
  it('should create an instance', () => {
    const directive = new ReduceApiCallsDirective();
    expect(directive).toBeTruthy();
  });
});
